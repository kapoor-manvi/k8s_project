apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ebs-sc-pvc
  namespace: external-secrets

spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ebs-sc
  resources:
    requests:
      storage: 4Gi